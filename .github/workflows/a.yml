on:
  workflow_dispatch:
  schedule:
    - cron: '48 13 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/me1en2e@cron
        with:
          token: ${{ secrets.token }}
